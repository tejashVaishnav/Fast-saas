/** @type {import('next').NextConfig} */
const nextConfig = { 
        images: {
            domains: ["lh3.googleusercontent.com", "vercel.com","dragonicon.blob.core.windows.net"],
          },
    
};

export default nextConfig;
